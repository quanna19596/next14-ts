'use client';

import { TLoginFormProps } from './LoginForm.d';

import './LoginForm.scss';

const LoginForm: React.FC<TLoginFormProps> = () => {
  return <div className='LoginForm'>LoginForm</div>;
};

export default LoginForm;
